const prompt = require('prompt-sync')();

let comidas = [];
for (let x = 1; x <=6; x ++){
    let comida = prompt('digite uma comida:');
    comida.push(comida);
}
console.log([0]);
console.log([4]);
comidas[6]= 'avatar 2'

